import React from 'react';

import { BoxContent } from 'components';
import { ContainerTemplate } from 'templates';

export const SkillsPage: React.FCC = () => (
  <BoxContent isContainer>
    <ContainerTemplate>Skills</ContainerTemplate>
  </BoxContent>
);
